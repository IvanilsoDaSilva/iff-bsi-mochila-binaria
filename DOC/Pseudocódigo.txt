INICIO
    // Criação de uma lista de itens com nome, peso e valor
    LISTA itens = [
        ITEM("Garfo", 1, 2),
        ITEM("Cálice", 3, 6),
        ITEM("Livro", 4, 8),
        ITEM("Lanterna", 2, 5),
        ITEM("Chave", 1, 3),
        ITEM("Bolsa", 5, 9),
        ITEM("Celular", 1, 7),
        ITEM("Carteira", 1, 4),
        ITEM("Relógio", 2, 6),
        ITEM("Óculos", 1, 5),
        ITEM("Faca", 2, 4),
        ITEM("Toalha", 3, 5),
        ITEM("Sapato", 4, 7),
        ITEM("Caderno", 2, 3),
        ITEM("Fone de Ouvido", 1, 6)
    ]

    // Definir a capacidade máxima da mochila
    CAPACIDADE mochilaCapacidade = 5

    // Definir o critério de parada do ILS (número de iterações)
    CRITERIO_DE_PARADA_ILS = 14

    // Definir o critério de parada da busca local
    CRITERIO_DE_PARADA_BUSCA_LOCAL = 100

    // Inicializar a mochila com os itens e a capacidade
    MOCHILA mochila = CRIAR_MOCHILA(itens, mochilaCapacidade)

    // Inicializar o método de busca local (ILS) com a mochila e os critérios de parada
    METODOS_ILS ils = CRIAR_METODOS_ILS(mochila, CRITERIO_DE_PARADA_ILS, CRITERIO_DE_PARADA_BUSCA_LOCAL)

    // Encontrar e exibir a solução
    SOLUCAO = ils.ENCONTRAR_SOLUCAO()
    ils.EXIBIR_SOLUCAO(SOLUCAO)

FIM
